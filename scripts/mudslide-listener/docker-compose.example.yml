services:
  mudslide-listener:
    build: .
    container_name: mudslide-listener
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - ./auth:/data/.mudslide